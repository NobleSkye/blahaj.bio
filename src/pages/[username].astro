---
// Redirect to @username format for consistency
const { username } = Astro.params;

if (username) {
  return Astro.redirect(`/@${username}`);
}

// If no username, redirect to home
return Astro.redirect('/');
---