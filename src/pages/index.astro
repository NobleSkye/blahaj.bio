---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { auth } from "../lib/auth";

const bios = await getCollection('bios');
const headers = Astro.request.headers;
const sessionRes = await auth.api.getSession({ headers });
const session = sessionRes?.data;
---

<Layout title="Blahaj.bio - Create Your Bio Page">
  <div class="min-h-screen flex flex-col">
    <main class="flex-grow bg-gradient-to-br from--100 to-purple-100 py-16">
      <div class="max-w-3xl mx-auto px-4">
        <div class="text-center mb-12">
          <h1 class="text-5xl font-bold text-gray-900 mb-4">
            Blahaj.bio
          </h1>
          <p class="text-xl text-white-700 mb-8">
            Create your personal bio page in seconds using GitHub Issues
          </p>
          <div class="space-y-4">
            <a
              href="/new"
              class="inline-block px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-500 text-white text-lg font-semibold rounded-lg hover:opacity-90 transition-opacity"
            >
              Get Started
            </a>
          </div>
        </div>

        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-8">
          <h2 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-6">How it works</h2>
          <div class="space-y-6">
            <div class="flex items-start gap-4">
              <div class="w-8 h-8 bg-blue-100 dark:bg-blue-600 rounded-full flex items-center justify-center shrink-0">
                <span class="text-blue-600 dark:text-blue-100 font-semibold">1</span>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100">Create an Issue</h3>
                <p class="text-gray-600 dark:text-gray-300">Click "Get Started" and fill out the bio template with your information</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-8 h-8 bg-purple-100 dark:bg-purple-600 rounded-full flex items-center justify-center shrink-0">
                <span class="text-purple-600 dark:text-purple-100 font-semibold">2</span>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100">Submit</h3>
                <p class="text-gray-600 dark:text-gray-300">GitHub Actions will automatically process your submission</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-8 h-8 bg-green-100 dark:bg-green-600 rounded-full flex items-center justify-center shrink-0">
                <span class="text-green-600 dark:text-green-100 font-semibold">3</span>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100">Done!</h3>
                <p class="text-gray-600 dark:text-gray-300">Your bio page will be available at blahaj.bio/@username</p>
              </div>
            </div>
          </div>
        </div>

        {session?.session ? (
          <div class="mt-8 text-center">
            <a href="/edit" class="inline-block px-4 py-2 rounded bg-blue-600 text-white">
              Edit your bio
            </a>
          </div>
        ) : (
          <div class="mt-8 text-center">
            <a href="/api/auth/sign-in?provider=github&callbackURL=/edit" class="inline-block px-4 py-2 rounded bg-green-600 text-white">
              Sign in with GitHub
            </a>
          </div>
        )}

        <div class="mt-8 grid gap-4">
          {bios.map(b => (
            <a href={`/@${b.data.username}`} class="p-4 rounded bg-gray-800 text-white">
              {b.data.display_name || b.data.username}
            </a>
          ))}
        </div>
      </div>
    </main>
  </div>
</Layout>
