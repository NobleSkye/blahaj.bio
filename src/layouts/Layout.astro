---
import Footer from '../components/Footer.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<html lang="en" class="dark">
  <head>
    <link rel="icon" type="image/svg" href="/favicon.svg" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body class="flex flex-col min-h-screen bg-github-canvas-default text-github-fg-default m-0">
    <header class="sticky top-0 left-0 right-0 bg-github-canvas-overlay border-b border-github-border-default p-4 shadow-sm z-10">
      <nav class="max-w-7xl mx-auto px-4 py-3">
        <a href="/" class="inline-block">
          <span class="text-2xl font-bold text-github-accent-fg hover:text-github-accent-emphasis transition-colors">
            Blähaj.bio
          </span>
        </a>
      </nav>
    </header>

    <main class="flex-grow">
      <slot />
    </main>

    <Footer />
  </body>
</html>
